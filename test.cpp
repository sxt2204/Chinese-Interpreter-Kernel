#include "./source/api.hpp"
#include <iostream>

using namespace std;

int main() {
    string chineseCode = R"(
定义函数 阶乘（数值）
    如果 数值 小于 2 那么
        返回 1
    返回 数值 乘 阶乘（数值 减 1）

整数 加法结果 等于 add（10，20）
输出并换行（"调用 lang/do/示例.json 中的 add 函数结果：" 加 加法结果）

文本 消息 等于 "Hello World"
整数 长度 等于 消息.获取长度（）
输出并换行（"调用 lang/do/示例.json 中的字符串.获取长度结果：" 加 长度）

双精度小数 平方根 等于 开平方根（16）
输出并换行（"开平方根（16）结果：" 加 平方根）

双精度小数 绝对值结果 等于 绝对值（-25）
输出并换行（"绝对值（-25）结果：" 加 绝对值结果）

双精度小数 最大值 等于 求最大值（15，42）
输出并换行（"求最大值（15，42）结果：" 加 最大值）

双精度小数 最小值 等于 求最小值（15，42）
输出并换行（"求最小值（15，42）结果：" 加 最小值）

双精度小数 向上取整结果 等于 向上取整（3.14）
输出并换行（"向上取整（3.14）结果：" 加 向上取整结果）

双精度小数 向下取整结果 等于 向下取整（3.99）
输出并换行（"向下取整（3.99）结果：" 加 向下取整结果）

整数 结果 等于 阶乘（5）
输出并换行（"5的阶乘计算结果为：" 加 结果）
    )";

    cout << "=== 运行包含 lang/do 自定义拓展功能的中文代码 ===" << endl;
    chinese_compiler::runDirect(chineseCode, true);

    return 0;
}