#include "./source/api.hpp"
#include <iostream>

using namespace std;

int main() {
    string chineseCode = R"(
定义函数 阶乘（数值）
    如果 数值 小于 2 那么
        返回 1
    返回 数值 乘以 阶乘（数值 减去 1）

整数 加法结果 设为 add（10，20）
打印并换行（"调用 add（10，20）结果：" 加 加法结果）

文本 消息 赋值为 "Hello World"
整数 长度 赋值为 消息 的 获取长度（）
打印并换行（"使用 '消息 的 获取长度（）' 结果为：" 加 长度）

双精度 平方根 设定为 开平方根（16）
打印并换行（"开平方根（16）结果：" 加 平方根）

双精度 绝对值结果 赋值为 绝对值（-25）
打印并换行（"绝对值（-25）结果：" 加 绝对值结果）

双精度 最大值 赋值为 求最大值（15，42）
打印并换行（"求最大值（15，42）结果：" 加 最大值）

双精度 最小值 赋值为 求最小值（15，42）
打印并换行（"求最小值（15，42）结果：" 加 最小值）

双精度 向上取整结果 赋值为 向上取整（3.14）
打印并换行（"向上取整（3.14）结果：" 加 向上取整结果）

双精度 向下取整结果 赋值为 向下取整（3.99）
打印并换行（"向下取整（3.99）结果：" 加 向下取整结果）

整数 结果 赋值为 阶乘（5）
打印并换行（"5的阶乘计算结果为：" 加 结果）
    )";

    cout << "=== 运行包含扩展词条与 '的' 成员调用的中文代码 ===" << endl;
    chinese_compiler::runDirect(chineseCode, true);

    return 0;
}