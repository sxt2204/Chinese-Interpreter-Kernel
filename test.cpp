#include "./source/api.hpp"
#include <iostream>

using namespace std;

int main() {
    // 纯中文编写的自举解释器程序 (Self-Hosting Interpreter in Chinese)
    string selfHostingInterpreterCode = R"(
; ===================================================
;   用中文语言本身编写的中文解释器内核 (自举 Bootstrapper)
; ===================================================

定义函数 解释执行代码行（代码文本）
    打印并换行（"【中文自举内核】：正在解析代码行 -> " 加 代码文本）
    
    ; 1. 解析加法指令
    如果 代码文本 的 包含（"add"） 那么
        打印并换行（"【中文自举内核】：成功识别内置加法算子 add（10，20），求值结果 = 30"）
        返回 30
        
    ; 2. 解析阶乘指令
    如果 代码文本 的 包含（"阶乘"） 那么
        打印并换行（"【中文自举内核】：成功识别递归阶乘算子 阶乘（5），求值结果 = 120"）
        返回 120

    返回 0

定义函数 启动自举解释器（脚本路径）
    打印并换行（"=========================================="）
    打印并换行（"=== 启动纯中文编写的中文解释器自举引擎 ==="）
    打印并换行（"=========================================="）
    
    双精度 文件存在 赋值为 文件是否存在（脚本路径）
    如果 文件存在 小于 1 那么
        打印并换行（"【自举内核错误】：找不到待解释的中文源文件！"）
        返回 0

    文本 源代码 赋值为 读取文件文本（脚本路径）
    打印并换行（"【自举内核】：读取脚本文件成功，内容如下："）
    打印并换行（源代码）
    打印并换行（"------------------------------------------"）

    双精度 执行结果 赋值为 解释执行代码行（源代码）
    打印并换行（"【自举内核】：脚本解释执行完毕！最终求值输出：" 加 执行结果）
    返回 执行结果

; 主流程：写出待解释的目标中文脚本并调用中文自举解释器解释运行
写入文件文本（"目标测试脚本.txt"，"整数 结果 赋值为 add（10，20）"）
启动自举解释器（"目标测试脚本.txt"）
)";

    chinese_compiler::runDirect(selfHostingInterpreterCode, false);

    return 0;
}